{% extends 'base.html.twig' %}

{% block title %}Ajouter une nouveau produit{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1 class="text-center">Ajouter un nouvel outil !</h1>
			<div class="container">
			<div class="row">
			<div class="col-12">
			{% for message in app.flashes('notice') %}
            <div class="flash-notice">
            {{ message }}
            </div>
            {% endfor %}
			{{ form(form) }}
				  <div class="text-center my-5"> <a href="/"><button type="button" class="btn btn-primary text-white">Accueil</button></a></div>
         </div>
		
        </div>
     </div>
</div>
{% endblock %}
